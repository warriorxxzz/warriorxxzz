import type { Project } from "../types";

// This is the ONLY file you edit to add a project.
// image, liveUrl, and status are all optional — the UI stays correct without them.
export const PROJECTS: Project[] = [];
